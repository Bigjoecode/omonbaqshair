# Normalize all text files to LF in the repo (deployed to Linux)
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.zip  binary
*.pdf  binary
